'''
두 비밀번호의 안전도 계산 - XOR ??
비트 연산 잘 모름

완전 탐색
- 두 비밀번호 중 동일한 비트가 같은 자리에 있다면 해당 자리는 XOR 값으로 고정 (0 ~ 최대값 범위 내에서) 
    - 즉, 같은 자리에 같은 비트가 있는 개수만큼 안전도 1 증가 
- 위에 해당되는 여러 후보 비밀번호에 대해 모두 안전도를 구함 

'''
# import sys
# input = sys.stdin.readline
# N = int(input())
# M = int(input())
# P = list(map(int, input().split()))


            
    
print(bin(6))



