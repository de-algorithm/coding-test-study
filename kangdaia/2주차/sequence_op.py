"""
n: 수의 개수 N(2 ≤ N ≤ 11)
seq: A_1, A_2, ..., A_N(1 ≤ A_i ≤ 100)로 구성된 수열 (정수) list
ops: 4개의 정수 list - 각 숫자는 덧셈, 뺄셈, 곱셈, 나눗셈의 갯수.

return
결과의 최댓값과 최솟값을 반환, -10억보다 크거나 같고, 10억보다 작거나 같은 결과가 나오는 입력만 주어짐
"""


def sequence_add_operator(n, seq, ops):
    return [0, 0]
